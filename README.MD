#googlePyMeets

About: This is a google meets bot running on selenium which will go to your classes for you.

Usage: To use replace the chromedriver.exe with the chrome driver supporting your google chrome browser version Then run python main.py and follow the prompts and questions.

Install Packages: pip install -r requirements.txt